node_modules/
plugin/dist/
plugin/node_modules/

# Runtime audit logs (per-agent; not committed)
.openclaw/

# Stale npm-pack snapshot from v1.0.0 (safe to delete but kept around for diff)
package/

# npm pack artifacts
*.tgz

# Locally-generated maintainer signing key (never commit)
.signing-key.ed25519.local
